# 2026-08-07

## Tindak lanjut cleanup rekomendasi audit kaki5
- Hapus dead code: `getPengaturan`/`setPengaturan` di `js/db.js`; `setExpDate` di `js/app-state.js` (setter tak terpakai). `expDate` **dipertahankan** karena masih dibaca `pengeluaran.js:30` untuk `tanggal` pengeluaran.
- `pos.js`: ganti `document.querySelector('.modal-overlay')` → `getElementById('cartModal')` di `openCartModal`/`closeCartModal` (lebih tidak rapuh; cartModal memang elemen `.modal-overlay` pertama).
- Dokumentasi disinkronkan: README/IDEA/DEVELOPER → 21 modul JS; README skema DB → 5 tabel aktif (menu, penjualan, pengeluaran, settings, platformMessages) + 1 legacy `pengaturan`; AUDIT-REPORT.md ditambah §7 + anotasi minor.
- Tambah `test_pos.js` (test `generatePresetNominal` via ekstrak fungsi murni dari source, gaya sama spt test_validate.js) — 6/6 lulus.
- Verifikasi: `node --check` db/app-state/pos ✅ · `test_pos.js` 6/6 ✅ · `test_validate.js` 14/14 ✅.

Catatan: seragamkan window-wiring (rec R3 audit) **ditunda** — tidak blocking & berisiko salah sentuh.

## UI redesign concept (UI Designer) — kaki5
- User minta redesign UI "baru, cantik, mudah" → pilih **full app redesign** + arah **modernized brand orange**.
- Deliverable: `kaki5-redesign.html` (prototype self-contained, satu file, bisa di-preview). Bukan mengubah prod langsung.
- Isi: design system (token warna/type/spacing/radius/shadow/motion + dark mode), 5 tab navigable (Beranda, Menu, Jualan/POS, Laporan, Pengaturan), kartu stat dgn hero "Omzet", bottom-sheet keranjang dgn stepper + hitung kembalian otomatis, chart laporan, toggle tema.
- Keputusan desain: brand `#E65100` (aksen/logo/aktif nav), tombol solid pakai `#C2410C` agar white-text lolos kontras WCAG AA; netral warm (no pure #000/#fff); font Plus Jakarta Sans (catatan: harus self-host di prod PWA offline, bukan CDN); touch target 44–48px; motion ease-out-quart + respect reduced-motion.
- Interaksi vanilla JS (addToCart, chg, setBayar, onBayar, simpan, setPeriod, renderReport, toast, carousel).
